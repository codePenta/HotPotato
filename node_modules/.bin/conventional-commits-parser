#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@commitlint/parse/node_modules/conventional-commits-parser/dist/cli/index.js" "$@"
  ret=$?
else 
  node  "$basedir/../@commitlint/parse/node_modules/conventional-commits-parser/dist/cli/index.js" "$@"
  ret=$?
fi
exit $ret
